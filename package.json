{
  "name": "mcp-music-studio",
  "version": "0.5.7",
  "type": "module",
  "description": "Two-mode MCP music studio: scored composition (ABC notation) and live performance (Strudel live coding). Interactive ext-apps widgets with sheet music rendering, in-place editing and MIDI/WAV export, all 128 General MIDI instruments, style presets, and a live REPL with Hydra shader visuals.",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "music",
    "abc-notation",
    "abcjs",
    "composition",
    "midi",
    "sheet-music",
    "strudel",
    "live-coding",
    "tidal-cycles",
    "ext-apps"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/linxule/mcp-music-studio"
  },
  "homepage": "https://github.com/linxule/mcp-music-studio#readme",
  "bugs": {
    "url": "https://github.com/linxule/mcp-music-studio/issues"
  },
  "license": "MIT",
  "mcpName": "io.github.linxule/mcp-music-studio",
  "author": "Xule Lin",
  "main": "dist/server.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun scripts/sync-version.mjs && tsc --noEmit && cross-env INPUT=mcp-app.html vite build && cross-env INPUT=strudel-app.html vite build && tsc -p tsconfig.server.json && bun build server.ts --outdir dist --target node && bun build src/shared/strudel-validate-child.ts --outdir dist --target node && bun build main.ts --outfile dist/index.js --target node --external \"./server.js\" --banner \"#!/usr/bin/env node\"",
    "test": "bunx vitest run",
    "test:watch": "bunx vitest",
    "watch": "cross-env INPUT=mcp-app.html vite build --watch",
    "serve": "bun --watch main.ts",
    "serve:stdio": "bun main.ts --stdio",
    "start": "cross-env NODE_ENV=development bun run build && bun run serve",
    "start:stdio": "cross-env NODE_ENV=development bun run build 1>&2 && bun run serve:stdio",
    "dev": "cross-env NODE_ENV=development concurrently \"bun run watch\" \"bun run serve\"",
    "prepublishOnly": "bun run build",
    "deploy": "bun run build && wrangler deploy",
    "preview": "bun run build && wrangler dev"
  },
  "engines": {
    "node": ">=20.11"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/core": "^2.0.0",
    "@modelcontextprotocol/ext-apps": "^2.0.0",
    "@modelcontextprotocol/sdk": "1.30.0",
    "@strudel/core": "1.2.6",
    "@strudel/mini": "1.2.6",
    "@strudel/tonal": "1.2.6",
    "@strudel/transpiler": "1.2.6",
    "abcjs": "^6.7.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "tonal": "^6.4.3",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.0",
    "@types/node": "26.5.1",
    "concurrently": "^10.0.5",
    "cross-env": "^10.1.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^5.0.0",
    "wrangler": "^4.131.2"
  },
  "types": "dist/server.d.ts",
  "exports": {
    ".": {
      "types": "./dist/server.d.ts",
      "default": "./dist/server.js"
    }
  },
  "bin": {
    "mcp-music-studio": "dist/index.js"
  }
}
