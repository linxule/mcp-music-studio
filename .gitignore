node_modules/
dist/
.mcpregistry_*

# Claude Code project config
CLAUDE.md
.claude/

# Local-only internal docs (audit reports, working notes) — not for the public repo
docs/internal/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# prevent root wrangler config (worker/ has its own)
/wrangler.jsonc
/wrangler.json
